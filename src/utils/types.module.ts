export interface tasksProps{
    titulo: string,
    nota: string,
    data: string,
    horaInicio: string,
    horaFim: string,
    repetir: string,
    cor: string,
    id: string,
    userId: number,
}
export type UserProps = {
    nome: string;
    id: string;
    email: string;
    senha: string;
};
